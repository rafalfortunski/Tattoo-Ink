import React from "react";
import Button from "../components/Button/Button";

const DesignSystem = () => {
  return (
    <>
      <Button className="button">Przycisk główny</Button>
      <Button className="button--is-main">Przycisk główny</Button>
    </>
  );
};

export default DesignSystem;
